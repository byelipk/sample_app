<%= form_for @post, 
	url: conversation_posts_path(@post.conversation_id),
	html: {id: 'response_post', class: 'response_post'}, 
	remote: true do |f| %>
	<%= f.hidden_field :prev_post_id %>
	<%= f.text_area :content, class: "first" %>
	<%= f.submit "Reply", id: "response" %>
<% end %>