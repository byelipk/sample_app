<% provide(:title, 'Create your Account') %>
<section class="static-wrapper">
    <% flash.each do |key, value| %>
        <%= content_tag(:div, value, 
                id: "flash-notification",
                class: "notice notice-#{key}") %>
    <% end %>
    <div class="form-header">       
        <span class="call-to-action">Sign up</span> 
        <span class="link-to"><%= link_to "Sign in", signin_path %></span>
    </div>   
    <div class="static-form-container">
        <%= form_for( @signup_form, url: users_path ) do |f| %>
            <%= render 'shared/error_messages', object: f.object %>
            <%= render 'users/user_partials/fields', f: f %>
            <%= render 'users/user_partials/submit_button', f: f %>
        <% end %>         
    </div>    
</section>