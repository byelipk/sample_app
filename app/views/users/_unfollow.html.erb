<!-- 
	The form_for() requires a model object to be passed in
	 in order to bind the form to the object. 

	 In this case, after the object is bound to the form, 
	 the form makes several parameters of the object available 
	 in the RelationshipsController destroy action, notably 
	 the relationship ID. 
	 								-->

<%= form_for(current_user.relationships.find_by_followed_id(@user), 
             html: { method: :delete }, remote: true ) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>