<header>
	<a href="<%= user_path(current_user) %>">
  		<%= gravatar_for current_user, size: 52 %>
	</a>
	<h6><%= link_to current_user.name, current_user %></h6>
	<h6><%= link_to "Edit Profile", edit_user_path(current_user) %></h6>
</header>
<span>
  	<%= pluralize(current_user.microposts.count, "micropost") %>
</span>