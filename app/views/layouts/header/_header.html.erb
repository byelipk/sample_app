<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "kshizzy", root_path, id: "logo" %>
      <nav>      
        <% if signed_in? %>
          <%= render 'layouts/header/signed_in_header' %>
        <% else %>         
          <%= render 'layouts/header/signed_out_header' %>
        <% end %>
      </nav>
    </div>
  </div>
</header>